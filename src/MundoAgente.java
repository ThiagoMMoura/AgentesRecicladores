/**
 *
 * A classe MundoAgente determina uma interface padrão para as classes de exibição
 * do ambiente do agente.
 */
public interface MundoAgente {
    public void atualizaCiclo(Ambiente ambiente);
}
